# --- Secretos / credenciales: NUNCA versionar ---
# Variables de entorno
.env
.env.*
!.env.example

# Claude Code: settings locales pueden contener API keys (mcpServers.env)
.claude/settings.local.json
.claude/settings.json

# Datos del skill banana (tracking de costos, presets con posibles keys)
.banana/

# Claves y credenciales sueltas
*.key
*.pem
*.p12
*.pfx
credentials.json
service-account*.json
*api*key*
*secret*

# Temporales / SO
.DS_Store
*.log
*.tmp
__pycache__/
*.pyc
